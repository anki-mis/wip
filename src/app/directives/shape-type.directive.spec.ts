import { ShapeTypeDirective } from './shape-type.directive';

describe('ShapeTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new ShapeTypeDirective();
    expect(directive).toBeTruthy();
  });
});
